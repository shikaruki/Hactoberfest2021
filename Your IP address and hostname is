# Aim: To find the IP address and hostname of your device
# Date : October 28, 2021
# Github : https://github.com/adarshaddee

import socket
hostname = socket. gethostname()
local_ip = socket. gethostbyname(hostname)
print(f"Your hostname  is {hostname} and IP address is {local_ip}")
